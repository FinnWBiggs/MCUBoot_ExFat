#include <autoconf.h>

auli_storage:
  address: 0x00
  affiliation: disk
  size: 0x1000
  region: external_flash
  extra_params: {
      disk_name: "NAND",
      disk_cache_size: 4096,
      disk_sector_size: 512,
      disk_read_only: 0
  }